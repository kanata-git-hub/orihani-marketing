# Kling 5초 영상 편집

영상 편집 탭에서 시나리오를 선택하고 Kling에서 받은 5초 MP4 한 개를 넣습니다. 해설 대본과 비용 확인란을 확인한 뒤 ‘음성·자동 싱크 준비’, 미리듣기와 자막 검토 후 ‘음성·자막 넣고 영상 완성’을 누릅니다.

- 제목: 첫 0~1초, 가로 중앙·위에서 25%.
- 자막: 가로 중앙·아래에서 25%. 화면 폭 약 75%에 맞춰 줄바꿈과 글자 크기를 계산합니다.
- 글꼴: 앱에 포함된 교보 손글씨 2024 박서우체. 서버에서도 같은 파일을 직접 읽습니다.
- 숫자 발음은 해설 음성에서만 한글로 바꾸며 자막 표기는 유지합니다.
- 등장인물 대사는 Kling 원본 음성을 보존합니다. 해설은 대사가 없는 시간에 배치합니다. 시간이 부족하면 자르지 않고 수정을 안내합니다.
- 시나리오의 장면 문구는 0~2초, 2~5초를 유지합니다. 해설·대사 자막은 실제 음성에서 찾은 단어 시간에 맞춥니다. 불확실한 자막은 확인 표시를 남깁니다.
- 시나리오별 편집과 영상, 분석 결과는 현재 브라우저에 저장됩니다. 탭을 바꿔도 편집기를 유지합니다. 기기가 바뀌면 파일을 다시 선택해야 합니다.
- 기존 대본·제목/태그·썸네일·고정 댓글 복사는 ‘휴대폰에 복사 / Kling 프롬프트’에 있습니다. 이전 Veo 결과는 하단 기록에서 저장할 수 있습니다.
- Veo 생성 UI와 활성 생성 코드는 제거했습니다. 이전 브라우저의 Veo 요청에는 410 안내를 반환합니다. 기존 영상 기록을 삭제하지 않습니다.

서버는 기존 Firebase 로그인을 검증하고 관리자 또는 승인된 사용자만 편집 API를 사용할 수 있습니다. Gemini 키는 기존 서버 설정을 사용합니다. 기본 음성 모델은 `gemini-3.1-flash-tts-preview`, 음성 분석은 `gemini-3.5-transcribe`입니다. 실패한 유료 생성 요청은 자동 재시도하지 않습니다.

FFmpeg가 포함된 Dockerfile로 배포합니다. Cloud Run은 메모리 2GiB, CPU 2, 요청 시간 제한 300초 이상을 권장합니다. 한 인스턴스에서는 한 번에 편집 작업 한 개를 처리합니다. 영상 한 파일 26MB 이하, 영상과 음성 합계 28MB 이하입니다.

검증: `npm run lint`, `npm run build`, `npm test`. `FFMPEG_PATH=ffmpeg npm test`는 실제 MP4 합성과 소리 유지 검증도 수행합니다. 테스트 파일은 `.editor-test`에 생성되며 배포에서 제외됩니다.
